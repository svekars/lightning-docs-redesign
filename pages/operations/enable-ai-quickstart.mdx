---
title: Enable AI in your Apps
description: Add AI to your apps instantly with Lightning model APIs. Send a web request to a model and pay only for the tokens you use.
---

# Enable AI in your Apps

Add AI to your apps instantly with Lightning model APIs. Send a simple web request to a model and only pay for the tokens you use. Every user gets 30 million free tokens per month.

First, install `litai` (or use the OpenAI Python SDK if you prefer):

```bash
pip install litai
```

Then call the model. Get your API key [here](https://lightning.ai/settings/api-keys).

```python
from litai import LLM

llm = LLM(model="openai/gpt-5", api_key="<LIGHTNING_API_KEY>")
print(llm.chat("Hello, world!"))
```

[Browse all 20+ models →](https://lightning.ai/models)
