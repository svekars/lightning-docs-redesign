---
title: Get started with Lightning AI
description: Create an account, open a Studio, and run your first AI workload in 5 minutes. No credit card required.
---

import { Cards, Steps, Tabs, Callout } from 'nextra/components'
import {
  LayoutTemplate, GitBranch, Cloud, Rocket, Server, Bot,
  MessageSquare, DollarSign, BookOpen
} from 'lucide-react'

# Get Started

Run your first AI workload in the cloud in 5 minutes. **No credit card needed.**

<Steps>

### Create an account

Go to **[lightning.ai/sign-up](https://lightning.ai/sign-up)** and create a free account.

<Callout type="info">
**No credit card required.** The free tier includes one always-on Studio, 100 GB of persistent storage, 30M model tokens per month, and free GPU hours once you verify.
</Callout>

### Open your first Studio

From the dashboard, click **New Studio**, or go directly to **[studio.lightning.ai](https://studio.lightning.ai)**.

Your Studio opens on free CPU. Switch to GPU anytime using the hardware selector in the top-right corner.

<Callout>
**What is a Studio?** A Studio is your cloud-based dev machine. It has persistent storage, your IDE in the browser (or local via SSH), and hardware you can swap in one click.
</Callout>

### Run your first example

Pick the path that matches what you brought with you:

<Cards>
  <Cards.Card icon={<LayoutTemplate />} title="Clone a template (recommended)" href="https://lightning.ai/svekars-org/templates" arrow>
    Browse featured templates, click Open in Studio, run. Fastest path to "I made something work."
  </Cards.Card>
  <Cards.Card icon={<GitBranch />} title="Connect your GitHub repo" href="https://lightning.ai/docs/platform/build/ai-studio" arrow>
    Bring your own code. Lightning pulls it into a Studio with dependencies installed.
  </Cards.Card>
  <Cards.Card icon={<Cloud />} title="Connect your GPU provider" href="https://lightning.ai/docs/platform/byoc" arrow>
    Use your own AWS, GCP, or other compute. Lightning manages the orchestration.
  </Cards.Card>
</Cards>

<Callout type="default">
**You just ran a real AI workload in the cloud.** From here, edit the code, swap to GPU hardware, or fork the template into your own project.
</Callout>

</Steps>

## Enable AI in your Apps: Quickstart

Lightning gives you several ways to start building with AI. Choose what you want to do:

<Tabs items={['Use a model (API)', 'Build with AI help', 'Prebuilt project', 'Cloud GPU', 'GPU notebook']}>
  <Tabs.Tab>
    **Use a model (API).** Add AI to your apps instantly with model APIs. Send a simple web request to a model and only pay for the tokens you use. Every user gets 30 million free tokens per month.

    First, install litAI (or use the OpenAI Python SDK if you prefer):

    ```bash
    pip install litai
    ```

    Then call the model. Get your API key [here](https://lightning.ai/settings/api-keys).

    ```python
    from litai import LLM

    llm = LLM(model="openai/gpt-5", api_key="<LIGHTNING_API_KEY>")
    print(llm.chat("Hello, world!"))
    ```

    [Browse all 20+ models →](https://lightning.ai/models)
  </Tabs.Tab>
  <Tabs.Tab>
    **Build with AI help.** The best entry point for beginners or anyone who wants assistance building AI.

    Create an account (or open [studio.lightning.ai](https://studio.lightning.ai) if you're already logged in), then use an AI copilot to build an agent.

    [Learn more about AI Studio →](https://lightning.ai/docs/platform/build/ai-studio)
  </Tabs.Tab>
  <Tabs.Tab>
    **Start from a prebuilt project.** Visit the environments hub and describe what you want. Lightning's AI search finds the closest template from a community of 350,000+ developers.

    [Visit the environments hub →](https://lightning.ai/templates)
  </Tabs.Tab>
  <Tabs.Tab>
    **Code on a cloud GPU.** Build, train, or deploy on a persistent, collaborative GPU workspace that feels like a local laptop, with AI copilots at every step. Every user gets ~80 free GPU hours per month.

    Start a Studio via [studio.lightning.ai](https://studio.lightning.ai) (defaults to CPU; upgrade to GPU later). Treat it like your laptop:

    1. Open VSCode or JupyterLab in the browser, or connect your local IDE (VSCode, Cursor, PyCharm) via SSH
    2. `git clone` your repo
    3. `pip install` (or `uv pip install`) dependencies
    4. Everything you install persists across restarts

    For inspiration, clone the starter project:

    ```bash
    git clone https://github.com/Lightning-AI/hello-studio.git
    cd hello-studio/rag-system
    uv pip install -r requirements.txt

    python scraper.py --page-limit 20 --update-every-hrs 24
    python rag_server.py
    python client.py "What is the Matrix?"
    ```

    [Learn more about AI Studio →](https://lightning.ai/docs/platform/build/ai-studio) · [Connect via SSH →](https://lightning.ai/docs/platform/build/ai-studio)
  </Tabs.Tab>
  <Tabs.Tab>
    **Create a GPU notebook.** Go to [studio.lightning.ai](https://studio.lightning.ai) and click the JupyterLab icon on the right side.

    [All about notebooks →](https://lightning.ai/docs/platform/build/notebooks)
  </Tabs.Tab>
</Tabs>

Once you're up and running, use **Ask AI** (bottom-right of any Studio) to explore whatever you want to do next.

## What to do next

<Cards num={3}>
  <Cards.Card icon={<Rocket />} title="Train a model on GPU" href="https://lightning.ai/docs/platform/train" arrow>
    Bring your own PyTorch or Lightning model. Train on GPUs from your free 80 hours/month.
  </Cards.Card>
  <Cards.Card icon={<Server />} title="Deploy a custom model" href="https://lightning.ai/docs/platform/inference" arrow>
    Wrap any inference engine with LitServe and deploy as a scalable API.
  </Cards.Card>
  <Cards.Card icon={<Bot />} title="Build an agent" href="https://lightning.ai/docs/platform/build/agents" arrow>
    Use AI copilots in a Studio to assemble agents that browse, run code, and chain reasoning.
  </Cards.Card>
</Cards>

## Got stuck?

<Cards>
  <Cards.Card icon={<MessageSquare />} title="Ask the community" href="https://discord.gg/MWAEvnC5fU" arrow>
    Lightning Discord, or use **Ask AI** in the bottom-right of any Studio.
  </Cards.Card>
  <Cards.Card icon={<DollarSign />} title="Pricing & plans" href="https://lightning.ai/pricing" arrow>
    Free tier details and upgrade paths if you need more.
  </Cards.Card>
  <Cards.Card icon={<BookOpen />} title="Why Lightning" href="/overview/why-lightning" arrow>
    How Lightning differs from traditional clouds.
  </Cards.Card>
</Cards>
